#!/bin/bash

set -e

chown -R scs:scs /scs-mnt/binlog
chown -R scs:scs /scs-mnt/replaylog
chown -R scs:scs /scs-mnt/data
[ ! -e /scs-mnt/data/mysql ] && /usr/bin/mysql_install_db --defaults-file=/scs/etc/mysqld/mysqld.ini

exec /usr/local/bin/supervisord -c /scs/etc/supervisor.conf
